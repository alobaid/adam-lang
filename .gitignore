# macOS Finder artifacts
.DS_Store

# The workspace's list for this folder's card — the board reads it, the published
# repository never carries it (the user's ruling, 2026-09-10: only the finished
# site is published).
TODO.md

# The workspace's manifest for this folder's card — the Oasis workspace's own, never
# public (the user's ruling, 2026-09-12). It was tracked from 2026-09-10 to 34ebbdf
# and is still in that history.
project.json
